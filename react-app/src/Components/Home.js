import styled from "styled-components";

const StyledHome = styled.main`
  background-color: red;
`;

const Home = () => {
  return (
    <StyledHome>
      <h1>Homepage</h1>
    </StyledHome>
  );
};

export default Home;
